# Core GUI and plotting dependencies
PyQt5>=5.15.0
matplotlib>=3.5.0
numpy>=1.20.0

# Development and testing
pytest>=6.0.0

# Optional: For better performance
pyqtgraph>=0.12.0

# Note: pyhidpp is included in the project under Vibration_test_scripts/pyhidpp/
# The project uses a local/embedded version of pyhidpp rather than PyPI version